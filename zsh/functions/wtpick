# wtpick — fuzzy-pick a live worktree and cd in. Re-enter by sight, not memory.

emulate -L zsh

local sel
sel=$(git worktree list | fzf --with-nth=1,3 --prompt="worktree> ") || return
cd "${sel%% *}"
