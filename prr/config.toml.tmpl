# prr — terminal-native PR reviews.
# This is a TEMPLATE. install.sh renders it to ~/.config/prr/config.toml (chmod 600),
# filling the token from `gh auth token` so no secret is ever committed to this repo.
# Never put a real token here.
[prr]
token = "__GITHUB_TOKEN__"
workdir = "__HOME__/reviews"
